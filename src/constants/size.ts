export enum LAYOUT_HEADER_HEIGHT {
  DESKTOP = 60,
  TABLET = 60,
  MOBILE = 60,
}

export enum LAYOUT_FOOTER_HEIGHT {
  DESKTOP = 282,
  TABLET = 282,
  MOBILE = 282,
}

export enum POST_GRID_COUNT {
  DESKTOP = 3,
  TABLET = 2,
  MOBILE = 1,
}

export const postMaxWidth = 768
